interface:
  display_name: "Project Check-in"
  short_description: "Turn a project update into the next safe action."
  default_prompt: "Use $project-check-in to process this update and propose the smallest confirmed state update."
