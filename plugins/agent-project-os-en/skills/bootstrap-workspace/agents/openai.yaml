interface:
  display_name: "Bootstrap Workspace"
  short_description: "Map a workspace without overwriting its memory."
  default_prompt: "Use $bootstrap-workspace to review this workspace and propose a safe Agent memory setup."
