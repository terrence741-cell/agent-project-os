# V1.0.0 验证摘要

## 发布证据

| 层级 | 结果 |
| --- | --- |
| L0 — 打包与可发现性 | 7/7 通过 |
| L1 — 本地安装与发现 | Codex 与 Claude Code 均已安装并展示 6 个 Skill；显式调用 Claude Code 及 Codex `$project-os` 均通过 |
| L2 — 单个 Skill 契约 | 10/10 通过^3 |
| L3 — 端到端场景 | 6/6 完整链路通过 |
| L4 — 安全回归 | R1--R11 通过 |

详细的匿名测试记录位于产品工作区，未随包分发：它们属于开发证据，不是运行时文件。

## Codex 调用检查

2026-07-31，在一次隔离、只读的 Codex CLI 会话中，以一家独立咖啡店新开展的 4 周短视频运营为例，调用了 `$project-os`。已安装插件在同一轮路由至 `launch-project`，返回了有用的临时运营型项目启动包，仅询问相关信息，并未创建或修改项目文件。

宿主发出警告：其 Skill 描述被压缩以适应 2% 的上下文预算。尽管如此，显式 `$project-os` 调用仍成功。本地宿主也在加载 Skill 前读取了其全局要求的 shared-memory 文件。此读取来自宿主的用户级策略，而非插件的路由指令；它被记录为环境交互，不被表述为「零读取路由器测试」的通过证据。

## 尚存的发布边界

这是审核候选版本，尚未公开发布。下一步外部动作是人工审阅此文件夹；获得批准后再进行 Git commit 与 push。
